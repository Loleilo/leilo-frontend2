import FlatButton from 'material-ui/FlatButton'

export default ({
                    permissions,
                    permissionsChanged,
                }) => {
    return (
        <div>

        </div>
    );
};